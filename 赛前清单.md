# 国赛 15 分钟现场清单

> 全部命令在 Mac 项目目录 `/Users/binggo/unitree` 下复制粘贴。
> 每条标定命令是全自动的：测量 → 自动调参 → 自动部署 → 自动杀旧进程。
> **只看输出最后一行**：`[PASS]`=下一步 / `[FIXED]`=已自动修好,下一步 / `[FAIL]`=按提示处理

## ⏱ 0:00-2:00 连通 (狗开机 + 网线)

```bash
ping -c 2 192.168.123.18
```

不通 → 查网线两头、狗电源。**通了才继续**。

## ⏱ 2:00-5:00 黄线标定

狗放赛道上，视野朝前能看到黄道（用遥控器摆位）：

```bash
.venv/bin/python scripts/onsite_calibrate.py lane
```

- `[PASS]` / `[FIXED]` → 下一步
- `[FAIL]` 且提示黄量太低 → 狗没对准黄道，挪一下重跑一次

## ⏱ 5:00-11:00 三个地标牌 (每个 ~2 分钟)

狗鼻子贴牌约 25cm（手搬狗最快），依次：

```bash
.venv/bin/python scripts/onsite_calibrate.py dump
.venv/bin/python scripts/onsite_calibrate.py stair
.venv/bin/python scripts/onsite_calibrate.py dock
```

- 每个 `[PASS]`/`[FIXED]` → 下一个
- `[FAIL]` D435i 抓帧失败 → USB 重插（蓝色口），再跑一次
- `[FAIL]` 深度无效 → 狗挪到离牌 25cm 左右，再跑一次
- 时间不够 → **dump 优先级最高**（卸料分最多），stair 第二，dock 可放弃（有超时兜底）

## ⏱ 11:00-13:00 启动

```bash
# 狗端 (ssh 密码 123)
ssh unitree@192.168.123.18
cd /home/unitree/go2-patrol && scripts/run_full_robot.sh
```

Mac 浏览器开 `http://192.168.123.18:8088/` 确认画面正常 + 右下 D435i 小窗有图。

## ⏱ 13:00-15:00 留给意外

| 现象 | 一条命令解决 |
|---|---|
| 狗朝场外走 | `lane` 标定重跑一次（它会自动切 lab）|
| 台阶误触发 | Mac 改 `config/params.yaml`: `night_blue_min_ratio` +0.06，然后 `sshpass -p 123 rsync -a config/params.yaml unitree@192.168.123.18:~/go2-patrol/config/ && sshpass -p 123 ssh unitree@192.168.123.18 "pkill -9 -f src.main"` 狗端重启 |
| 狗不动 | 检查 App 里 sport_mode 开了没（mcf 要关）|
| D435i 全程没图 | 拔插 USB；还不行就认了——程序会自动退化到纯鱼眼模式，能跑 |
| 代码炸了 | `git reset --hard origin/main` 然后 rsync src/ + config/ 上狗 |

## 心法

1. 标定命令**可以重复跑**，跑坏了不会更糟
2. 每个输出**只看最后一行**，不用读中间的数字
3. 实在来不及标定 → 直接跑。昨晚的配置在 3 个场地验证过，是能跑的底线
