# 国赛现场操作清单 (按顺序执行)

> Mac 和狗用网线连好, Mac 在项目目录 `/Users/binggo/unitree` 下操作。
> 狗密码 123。所有命令直接复制粘贴。

## 0. 到场先跑 (2 分钟)

```bash
# 连通测试
ping -c 2 192.168.123.18

# 确认狗上代码是最新 (输出应含 lane_detector)
sshpass -p 123 ssh unitree@192.168.123.18 "grep lane_detector ~/go2-patrol/config/params.yaml"
```

不通 → 查网线 / 狗电源。

## 1. 黄线标定 (5 分钟, 最重要)

把狗放在赛道上, **视野里同时有黄道和场外地板**:

```bash
.venv/bin/python scripts/onsite_calibrate.py lane
```

看输出的两张图 (`logs/onsite/lane_hsv_*.jpg` / `lane_lab_*.jpg`):

| 情况 | 动作 |
|---|---|
| HSV 图干净 (黄道亮+地板黑) | 啥都不用做, 直接比赛 |
| HSV 误判地板, LAB 干净 | 改 `config/params.yaml`: `lane_detector: lab` → 跑下面的"推配置" |
| 两个都不行 | 把图发给 AI |

推配置 + 重启:

```bash
sshpass -p 123 rsync -a config/params.yaml unitree@192.168.123.18:~/go2-patrol/config/
sshpass -p 123 ssh unitree@192.168.123.18 "pkill -9 -f src.main"
# 然后狗端重新 scripts/run_full_robot.sh
```

## 2. 三个地标牌标定 (每个 2 分钟)

狗鼻子贴牌 ~25cm, 依次跑:

```bash
.venv/bin/python scripts/onsite_calibrate.py dump    # 倾倒区白圆黑字
.venv/bin/python scripts/onsite_calibrate.py stair   # 台阶牌
.venv/bin/python scripts/onsite_calibrate.py dock    # 充电牌
```

每个都显示 `✓ 能正确触发` 就过。显示 ✗ 就把打印的 w/k/b 数字发给 AI。

## 3. 试跑 (有时间就做)

```bash
# 狗端
cd ~/go2-patrol && scripts/run_full_robot.sh
```

Mac 浏览器开 `http://192.168.123.18:8088/` 实时看。

## 4. 出问题速查

| 现象 | 第一反应 |
|---|---|
| 狗朝场外/地板走 | lane_detector 切 lab (见步骤1) |
| 地标牌不触发 | 跑对应 onsite_calibrate 看 w/k/b |
| 误触发台阶 | yaml `night_blue_min_ratio` 调大 (0.12→0.18) |
| 狗不动 | 速度参数 ≥0.22 (sport_mode 门槛); 检查 mcf/sport 模式 |
| D435i 失效 | `lsusb \| grep -i intel`; 重插 USB3 (蓝口) |
| 录像段错误 (segfault) | 命令前要 `export LD_LIBRARY_PATH` (onsite 脚本已内置) |
| 代码全没了 | `git fetch origin && git reset --hard origin/main` |

## 5. 启动比赛

```bash
# 狗端
cd /home/unitree/go2-patrol
scripts/run_full_robot.sh
```

## 万一要回省赛版

```bash
# 狗端有备份的话
cd ~/go2-patrol-provincial-backup && scripts/run_full_robot.sh
```
